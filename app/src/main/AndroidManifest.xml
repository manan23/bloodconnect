<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.bloodconnect">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/imagesicon1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/imagesicon1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.register" />
        <activity android:name=".activity.MainActivity" />
        <activity
            android:name=".activity.dashboard"
            android:label="@string/title_activity_dashboard"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activity.finddonar"
            android:label="Find Donar"
            android:theme="@style/AppTheme1" />
        <activity
            android:name=".activity.profile"
            android:label="Profile"
            android:theme="@style/AppTheme1" />
        <activity android:name=".activity.feedbackform"></activity>
    </application>

</manifest>